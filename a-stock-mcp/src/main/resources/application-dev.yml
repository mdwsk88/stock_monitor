server:
  port: 8091
spring:
  datasource: # 数据源的相关配置
    type: com.zaxxer.hikari.HikariDataSource      # 数据源的类型
    driver-class-name: com.mysql.cj.jdbc.Driver   # mysql的数据库驱动类名称
    url: ${A_STOCK_MCP_DB_URL}
    username: ${A_STOCK_MCP_DB_USERNAME}
    password: ${A_STOCK_MCP_DB_PASSWORD}
    hikari:
      pool-name: DataSourceHikariCP               # 连接池的名字
      connection-timeout: 30000                   # 等待连接池分配连接的最大时间（毫秒）
      minimum-idle: 5                             # 最小连接数
      maximum-pool-size: 20                       # 最大连接数
      auto-commit: true                           # 自动提交
      idle-timeout: 600000                        # 连接超时的最大时长（毫秒）
      max-lifetime: 18000000                      # 连接池的最大生命时长（毫秒）
      connection-test-query: SELECT 1

server:
  port: 8091
spring:
  datasource: # 数据源的相关配置
    type: com.zaxxer.hikari.HikariDataSource      # 数据源的类型，可以更改为其他的数据源配置，比如druid
    driver-class-name: com.mysql.cj.jdbc.Driver      # mysql/MariaDB 的数据库驱动类名称
    url: ${A_STOCK_MCP_DB_URL}
    username: ${A_STOCK_MCP_DB_USERNAME}
    password: ${A_STOCK_MCP_DB_PASSWORD}
    hikari:
      pool-name: DataSourceHikariCP           # 连接池的名字
      connection-timeout: 30000               # 等待连接池分配连接的最大时间（毫秒），超过这个时长还没有可用的连接，则会抛出SQLException
      minimum-idle: 1                         # 保持少量热连接，降低休眠唤醒后的僵尸连接概率
      maximum-pool-size: 20                   # 最大连接数
      auto-commit: true                       # 自动提交
      idle-timeout: 300000                    # 让多余空闲连接更快退场，减少拿到僵尸连接的概率
      max-lifetime: 840000                    # 小于数据库/代理侧空闲断链阈值，主动淘汰老连接
      keepalive-time: 240000                  # 定期保活最小空闲连接，避免长时间静默后被对端重置
      validation-timeout: 5000                # 连接校验超时时间
      connection-test-query: SELECT 1
      data-source-properties:
        connectTimeout: 10000                 # 数据库连接建立超时，避免网络恢复阶段长时间卡死
        socketTimeout: 60000                  # 数据库读写超时，避免休眠唤醒后请求无限挂起
        tcpKeepAlive: true                    # 让驱动参与 TCP 保活，降低长连接静默断链概率

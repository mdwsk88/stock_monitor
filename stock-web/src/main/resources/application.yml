spring:
  application:
    name: us-stock-monitor-web
  profiles:
    active: dev
logging:
  level:
    root: info


# MyBatisPlus 的配置
mybatis-plus:
  global-config:
    db-config:
      id-type:
      update-strategy: not_empty
  mapper-locations: classpath*:/mappers/*.xml

# 企业微信机器人 Webhook 配置（支持多市场）
# 美股推送
WECOM_WEBHOOK_URL_US: ${WECOM_WEBHOOK_URL_US:}
# A股推送
WECOM_WEBHOOK_URL_A: ${WECOM_WEBHOOK_URL_A:}
# 港股推送（预留）
WECOM_WEBHOOK_URL_HK: ${WECOM_WEBHOOK_URL_HK:}
# 兼容旧配置：如果以上配置为空，将使用此默认配置
WECOM_WEBHOOK_URL: ${WECOM_WEBHOOK_URL:}

server:
  port: 8080
spring:
  datasource: # 数据源的相关配置
    type: com.zaxxer.hikari.HikariDataSource      # 数据源的类型，可以更改为其他的数据源配置，比如druid
    driver-class-name: com.mysql.cj.jdbc.Driver      # mysql/MariaDB 的数据库驱动类名称
    url: jdbc:mysql://<nas-host>:3307/stock_monitor?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Mdwsk881991qwe[]
    hikari:
      pool-name: DataSourceHikariCP           # 连接池的名字
      connection-timeout: 30000               # 等待连接池分配连接的最大时间（毫秒），超过这个时长还没有可用的连接，则会抛出SQLException
      minimum-idle: 5                         # 最小连接数
      maximum-pool-size: 20                   # 最大连接数
      auto-commit: true                       # 自动提交
      idle-timeout: 600000                    # 连接超时的最大时长（毫秒），超时则会被释放（retired）
      max-lifetime: 18000000                  # 连接池的最大生命时长（毫秒），超时则会被释放（retired）
      connection-test-query: SELECT 1

